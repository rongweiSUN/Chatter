---
description: 记录每次对话中 AI 做了什么以及用户反馈
alwaysApply: true
---

# 变更日志规则

每次对话结束前（或完成一个有意义的改动后），必须在 `docs/changelog.md` 末尾追加一条记录。

## 记录格式

```markdown
## YYYY-MM-DD

### 做了什么
- 简要描述本次改动（每条一行）

### 用户反馈
- 用户对结果的评价或后续要求（如本次无明确反馈则写"无"）
```

## 规则

- 只追加，不修改或删除已有记录
- 描述要具体：写清改了哪些文件、实现了什么功能、修了什么 bug
- 用户反馈如实记录：满意、不满意、要求返工、提出新需求等
- 如果一次对话有多轮改动，可合并为一条记录
